export default async function FormsPage() {
  return (
    <section className="rounded border bg-white p-4 text-sm text-gray-600">
      Form tracking can be added with custom events (submit/start). Add those
      events and this report will populate automatically.
    </section>
  );
}
