export default async function MediaInsightsPage() {
  return (
    <section className="rounded border bg-white p-4 text-sm text-gray-600">
      Media insights can be added using custom events (play, pause, completion).
      Once tracked, this report will populate.
    </section>
  );
}
