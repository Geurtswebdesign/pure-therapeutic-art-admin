export default async function SearchConsolePage() {
  return (
    <section className="rounded border bg-white p-4 text-sm text-gray-600">
      Search Console data requires an external integration (Google Search Console).
      We can connect this later and render impressions, clicks, and queries here.
    </section>
  );
}
